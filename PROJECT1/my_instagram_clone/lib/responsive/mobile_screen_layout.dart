import 'package:flutter/material.dart';

class MobileViewWidget extends StatelessWidget {
  const MobileViewWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text("web view"));
  }
}
